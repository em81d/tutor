# Copy this file to .env (project root) and fill in your Supabase project's values.
# Project Settings -> API in the Supabase dashboard.
# The anon key is safe to expose to the browser -- it's the public key, gated by RLS policies.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
